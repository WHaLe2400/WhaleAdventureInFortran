### 面向对象的编成

通过在定义数据的时候使用`public`实现数据在不同子程序内被调用，
将类内的数据绑定在过程中

#### Type
包含属性区、行为区。

#### 关系运算（看起来和C一模一样）
*但是其中的不等于是*`/=`
- 对于相等的判断受限于编译器，最好加入一个小一点的误差容忍

#### 逻辑表达式
可以对两个逻辑型变量进行运算

包含：
`.AND.`
`.OR.`
`.NEQV.`
......

Selec case的判断不能使用浮点数与复数